<div>
  <h5>Comment by <%= comment.author_name %><span>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</span></h5>
  <p class="comment"><%= comment.body %></p>
  
 </div>